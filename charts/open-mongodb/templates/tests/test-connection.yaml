apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-mongodb.fullname" . }}-test-connection"
  labels:
    {{- include "open-mongodb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "open-mongodb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
