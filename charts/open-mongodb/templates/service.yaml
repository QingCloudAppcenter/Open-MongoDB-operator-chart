apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-mongodb.fullname" . }}
  labels:
    {{- include "open-mongodb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-mongodb.selectorLabels" . | nindent 4 }}
